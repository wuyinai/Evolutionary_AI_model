---
alwaysApply: false
description: 当编写新的后端接口方法时触发
---
1. 所有接口方法必须添加权限注解，权限注解为例如 `@PreAuthorize("hasAuthority('ai:config:list')")`
2. 权限注解中的权限字符串必须与系统权限字符串一致，例如 `ai:config:list` 为 `list` 操作的权限字符串。
3. 权限注解必须添加在接口方法上，不能添加在类上，且一个方法一个权限字符串。
4. 每次添加新的权限字符串，都要编写对应的插入数据库sql语句。
