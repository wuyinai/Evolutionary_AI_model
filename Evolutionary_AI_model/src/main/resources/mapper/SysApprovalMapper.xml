<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.evolutionary_ai_model.mapper.SysApprovalMapper">

    <select id="selectByApprovalType" resultType="com.example.evolutionary_ai_model.entity.SysApproval">
        SELECT * FROM sys_approval
        WHERE approval_type = #{approvalType} AND del_flag = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectByApplicantId" resultType="com.example.evolutionary_ai_model.entity.SysApproval">
        SELECT * FROM sys_approval
        WHERE applicant_id = #{applicantId} AND del_flag = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectByApprovalStatus" resultType="com.example.evolutionary_ai_model.entity.SysApproval">
        SELECT * FROM sys_approval
        WHERE approval_status = #{approvalStatus} AND del_flag = 0
        ORDER BY create_time DESC
    </select>

</mapper>